package com.grabduck.taskmanager.domain;

public enum TaskPriority {
    URGENT,    // For critical, time-sensitive tasks
    HIGH,      // Important but not urgent
    MEDIUM,    // Normal priority
    LOW        // Can be done when time permits
}
